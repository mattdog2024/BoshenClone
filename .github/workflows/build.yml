name: 打包 Windows EXE

# 触发条件：每次推送到 main 分支，或者手动点击触发
on:
  push:
    branches:
      - main
  workflow_dispatch:  # 允许在 GitHub 网页上手动触发

jobs:
  build:
    runs-on: windows-latest  # 使用 GitHub 提供的 Windows 服务器

    steps:
      # 第1步：拉取最新代码
      - name: 拉取代码
        uses: actions/checkout@v4

      # 第2步：安装 Python 3.11
      - name: 安装 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 第3步：安装所有依赖
      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install PySide6
          pip install rapidocr-onnxruntime
          pip install numpy opencv-python
          pip install pyinstaller

      # 第4步：用 PyInstaller 打包成单文件 exe
      - name: PyInstaller 打包
        run: |
          pyinstaller --noconfirm --onefile --windowed `
            --name "BoshenKaiLine_v3.2.9" `
            --hidden-import="rapidocr_onnxruntime" `
            --collect-all="rapidocr_onnxruntime" `
            main.py

      # 第5步：把打包好的 exe 上传为 Artifact（可在 Actions 页面下载）
      - name: 上传 EXE
        uses: actions/upload-artifact@v4
        with:
          name: BoshenKaiLine_v3.2.9
          path: dist/BoshenKaiLine_v3.2.9.exe
          retention-days: 30  # 保留30天
